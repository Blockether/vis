# Every dependency in package.json is pinned to one exact version: no `^`, no `~`.
# `npm install <pkg>` would otherwise write a caret range and let a transitive
# minor slip into a release build between `npm ci` runs on CI and on a machine.
save-exact=true
save-prefix=
