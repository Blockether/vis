{
  // The tests additionally use node globals (Buffer) to verify real signatures.
  "extends": "./tsconfig.base.json",
  "compilerOptions": { "types": ["@cloudflare/workers-types", "node"] },
  "include": ["src", "test", "vitest.config.ts"]
}
