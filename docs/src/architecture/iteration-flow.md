# Iteration Flow

Each iteration asks the model for Clojure forms, evaluates them, records results in the journal, and either continues or returns an answer.

Verification is command/tool output in the journal.
