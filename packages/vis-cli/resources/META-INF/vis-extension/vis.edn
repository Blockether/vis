;; vis-cli's classpath registrations: built-in CLI commands (run,
;; auth, doctor, conversations, extensions). Non-LLM-facing, so no
;; :docs map -- the id is purely organizational.
{vis-cli
 {:nses [com.blockether.vis-cli.channels.cli]}}
