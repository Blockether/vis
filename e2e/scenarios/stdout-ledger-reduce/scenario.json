{
  "lang": "python",
  "prompt": "First, in one python_execution block print the large source result by running: `case = \"ledger\"; exec(Path(project_root_path / \"emit.py\").read_text())`. Do not inspect emit.py or compute an answer before this first result. Once each result has appeared, recover the exact saved stdout of each original call using its clipping hint (original scope AND tool-call ID). Do not rerun the emitter or reuse its original rows. In a later Python block for EACH output print `LEN: <character count>`, `SHA256: <UTF-8 digest>`, and the requested target data row verbatim (not the clipping hint); use the recovered stdout to compute the answer. Print only concise evidence, not the full output again. Print the line containing row:03100. Process the whole ledger: for account beta, sum amounts of settled rows and count reversed rows. Return exactly the JSON keys beta_settled_net and beta_reversed_count, without an evidence field. Respond ONLY with a JSON object, no prose or code fence.",
  "want": {},
  "wantnot": {},
  "want_answer_json": {
    "beta_settled_net": 12729,
    "beta_reversed_count": 119
  },
  "want_forms": [
    "read_session()"
  ],
  "want_stdout_recovery": {
    "min_chars": 120000,
    "head": "LEDGER-HEAD",
    "middle": "row:03100|acct=alpha|amount=-5|status=settled",
    "tail": "LEDGER-TAIL",
    "goal_status": null
  },
  "want_requested_route": true,
  "timeout_s": 600,
  "files_from": "stdout-lookup-middle"
}
