{
  "lang": "clojure",
  "prompt": "Rename the Clojure namespace foo.bar to foo.core across the whole project: the ns declaration, the :require target, and every qualified usage, while keeping the local :as alias. Locate the references, read their anchored source and edit them with patch. Then move src/foo/bar.clj to src/foo/core.clj; do not read src/foo/core.clj until after the move. Verify the moved source and every reference.",
  "want": {
    "src/app.clj": ["foo.core/h 1", "fb/h 2", "[foo.core :as fb]"],
    "src/foo/core.clj": ["(ns foo.core)", "(defn h [x] (inc x))"]
  },
  "wantnot": {
    "src/app.clj": ["foo.bar"]
  },
  "want_tools": ["patch"]
}
