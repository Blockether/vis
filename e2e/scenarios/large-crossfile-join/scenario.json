{
  "lang": "python",
  "prompt": "Join the files at project_root_path / 'data' / 'accounts.csv' and project_root_path / 'data' / 'events.csv' on account_id. In python_execution, resolve both from project_root_path, not relative to the current working directory. The accounts columns are account_id,tier,region; the events columns are event_id,account_id,kind,latency. The events.kind marks failed events (kind=failed); there is no status column. Among failed events whose account is gold tier AND east region, compute failed_events, affected_accounts (distinct account IDs), and top_three objects {account_id, failures} sorted by descending failures then ascending account_id. Do not print raw rows or whole files; keep each tool stdout under 900 characters. Answer only with the exact JSON object.",
  "want": {},
  "wantnot": {},
  "want_answer_json": {
    "failed_events": 171,
    "affected_accounts": 145,
    "top_three": [
      {
        "account_id": "acct-0078",
        "failures": 2
      },
      {
        "account_id": "acct-0090",
        "failures": 2
      },
      {
        "account_id": "acct-0186",
        "failures": 2
      }
    ]
  },
  "max_form_output_chars": 900,
  "max_total_output_chars": 1800,
  "want_requested_route": true,
  "fixture_generator": "generate.py",
  "timeout_s": 600,
  "files_from": "large-csv-groups"
}
