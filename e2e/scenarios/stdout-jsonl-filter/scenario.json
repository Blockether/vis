{
  "lang": "python",
  "prompt": "First, in one python_execution block print the large source result by running: `case = \"jsonl\"; exec(Path(project_root_path / \"emit.py\").read_text())`. Do not inspect emit.py or compute an answer before this first result. Once each result has appeared, recover the exact saved stdout of each original call using its clipping hint (original scope AND tool-call ID). Do not rerun the emitter or reuse its original rows. In a later Python block for EACH output print `LEN: <character count>`, `SHA256: <UTF-8 digest>`, and the requested target data row verbatim (not the clipping hint); use the recovered stdout to compute the answer. Print only concise evidence, not the full output again. Print the line containing rid:02200. Parse the JSONL rows, excluding the HEAD/TAIL labels. For region east and priority high with latency >= 900, count matches. Return only matched_count and top_three; each top_three object has id (the complete rid:NNNNN string) and latency, sorted by descending latency then ascending id. Respond ONLY with a JSON object, no prose or code fence.",
  "want": {},
  "wantnot": {},
  "want_answer_json": {
    "matched_count": 84,
    "top_three": [
      {
        "id": "rid:00853",
        "latency": 994
      },
      {
        "id": "rid:03937",
        "latency": 994
      },
      {
        "id": "rid:02227",
        "latency": 991
      }
    ]
  },
  "want_forms": [
    "read_session()"
  ],
  "want_stdout_recovery": {
    "min_chars": 120000,
    "head": "JSONL-HEAD",
    "middle": "rid:02200",
    "tail": "JSONL-TAIL",
    "goal_status": null
  },
  "want_requested_route": true,
  "timeout_s": 600,
  "files_from": "stdout-lookup-middle"
}
