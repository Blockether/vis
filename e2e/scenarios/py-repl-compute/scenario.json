{
  "lang": "python",
  "prompt": "The project has a module secret.py with a function compute(). Start the project Python REPL with repl_start, then use repl_eval to import the module and call compute(). Do not execute the module in the sandbox, use shell, or reimplement it by hand. Tell me exactly what compute() returns, and stop the REPL you started with repl_stop when finished.",
  "want": {},
  "wantnot": {},
  "want_answer": ["338350"],
  "want_tools": ["repl_eval"]
}
