{
  "lang": "python",
  "prompt": "The following contracts are known from previous work and unchanged; reuse them without apropos, doc, signature inspection or contract metadata reads. The preinstalled contract_probe.status() takes no arguments and returns records and total without changing the local ledger. contract_probe.record(person, /, *, bucket, copies=2, note=None) appends a local receipt, never sends a message, requires nonblank person and bucket and positive copies, and returns sequence, person, bucket, copies, note and total. Person is positional-only; bucket, copies and note are keyword-only. Use the preinstalled contract_probe extension to check the initial totals, then record Ada in inbox using the normal copy count and no note, then record Lin in archive with three copies and the note urgent. Finally read the totals and report the two receipts and final totals. Do not read source files, import the extension, recreate it, use shell, or write files yourself. Only the registered extension may change its local ledger. If the extension is unavailable, report that and stop. Return only a JSON object with keys receipts (the two complete receipt objects, each with sequence, person, bucket, copies, note and total) and final_totals (an object with records and total). Do not add prose or other keys.",
  "want": {},
  "wantnot": {},
  "want_tools": [
    "contract_probe.record",
    "contract_probe.status"
  ],
  "max_form_output_chars": 6000,
  "want_requested_route": true,
  "want_json_files": {
    "contract-receipts.jsonl": [
      {
        "bucket": "inbox",
        "copies": 2,
        "note": null,
        "person": "Ada",
        "sequence": 1,
        "total": 2
      },
      {
        "bucket": "archive",
        "copies": 3,
        "note": "urgent",
        "person": "Lin",
        "sequence": 2,
        "total": 5
      }
    ],
    "contract-calls.jsonl": [
      {
        "operation": "contract_probe.status",
        "arguments": {}
      },
      {
        "operation": "contract_probe.record",
        "arguments": {
          "person": "Ada",
          "bucket": "inbox",
          "copies": 2,
          "note": null
        }
      },
      {
        "operation": "contract_probe.record",
        "arguments": {
          "person": "Lin",
          "bucket": "archive",
          "copies": 3,
          "note": "urgent"
        }
      },
      {
        "operation": "contract_probe.status",
        "arguments": {}
      }
    ]
  },
  "want_answer_json": {
    "receipts": [
      {
        "bucket": "inbox",
        "copies": 2,
        "note": null,
        "person": "Ada",
        "sequence": 1,
        "total": 2
      },
      {
        "bucket": "archive",
        "copies": 3,
        "note": "urgent",
        "person": "Lin",
        "sequence": 2,
        "total": 5
      }
    ],
    "final_totals": {
      "records": 2,
      "total": 5
    }
  },
  "want_activity_sequence": [
    "contract_probe.status",
    "contract_probe.record",
    "contract_probe.record",
    "contract_probe.status"
  ],
  "discovery": {
    "signatures": [],
    "contracts": [],
    "known": true
  },
  "want_cache_metrics": true,
  "max_total_output_chars": 10000,
  "forbid_tools": [
    "cat",
    "grep",
    "ls",
    "shell",
    "patch"
  ],
  "files_from": "extension-contract-discovery"
}
