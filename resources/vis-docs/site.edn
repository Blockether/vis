;; The docs SITE — hero copy and the ordered navigation of the manual.
;; Nothing here is searchable: a page's RECORD is in META-INF/vis/apropos/docs.edn,
;; and THIS is the only place that titles a page, groups it and orders it. The
;; vector order IS the order — there are no order numbers to keep in step.
{:site
 {:title "Vis",
  :tagline "A coding agent that keeps its state in a running program, not in the chat.",
  :repo "https://github.com/Blockether/vis",
  :headline "Context is an environment, not a transcript.",
  :sub "Forty steps into a task, most agents are still paying for step one. Vis holds state in a live runtime the model talks to through code — vars, a database, query results — and pulls the handful of values it actually needs. Works with any text model.",
  :cta {:label "Get started", :slug "token-optimization"}}

 :nav
 [
  {:section nil
   :pages
   [{:page "index", :title "Introduction", :blurb "What Vis is and the context-as-environment idea — start here."}]}
  {:section "How it works"
   :pages
   [{:page "token-optimization", :title "Token optimization", :blurb "How Vis keeps the context window small by holding state in a live runtime instead of the transcript."}
    {:page "graalpython", :title "GraalPython sandbox", :blurb "The GraalPython sandbox that executes the model's Python tool code."}
    {:page "jail", :title "Process jail & egress", :blurb "The OS jail and gateway egress proxy: filesystem admission, network policy, managed processes, and how to verify them."}
    {:page "content-blocks", :title "Content-block protocol", :blurb "The canonical role-labelled message, typed block, persistence, and streaming contract."}]}
  {:section "Runtime"
   :pages
   [{:page "jvm-native-image", :title "JVM & native-image", :blurb "Running Vis on the JVM and building the standalone native-image binary."}
    {:page "distributions", :title "Runtime distributions", :blurb "Runtime selection (native, release-tagged source), update channels, wrapper bundles, and platform builds."}]}
  {:section "Using Vis"
   :pages
   [{:page "configuration", :title "Configuration", :blurb "Config files, providers, and models — how to set Vis up."}
    {:page "context-and-prompts", :title "Context files & prompts", :blurb "AGENTS.md/CLAUDE.md project context files and prompt customization."}
    {:page "exporting-sessions", :title "Exporting sessions", :blurb "Exporting and sharing past sessions and forensic reports."}
    {:page "skills", :title "Skills", :blurb "On-demand skills the agent loads only when a task needs them."}
    {:page "queue-and-cancel", :title "Queue, cancel & quit", :blurb "Queueing follow-up messages, auto-drain on resume, cancelling a turn, and Ctrl+C quit."}
    {:page "gateway", :title "Gateway, pairing & remote access", :blurb "The gateway server, companion pairing, and driving a gateway on another machine from the CLI."}
    {:page "reporting-bugs", :title "Reporting bugs safely", :blurb "How to file a safe bug report: what to include, what never to paste, and how to sanitize a session export."}]}
  {:section "Extensions"
   :pages
   [{:page "extending", :title "Extending Vis", :blurb "One guide to extending Vis: drop-in Python extensions and full-surface Clojure extensions."}]}
 ]}
