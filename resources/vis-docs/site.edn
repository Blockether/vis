{:nav
 [{:pages
   [{:blurb "Install Vis and run your first session." :page "index" :title "Getting started"}]
   :section nil}
  {:pages
   [{:blurb "Set up providers, models and project settings."
     :page "configuration"
     :title "Configuration"}
    {:blurb "Tell the agent how your codebase works."
     :page "context-and-prompts"
     :title "Project instructions"}
    {:blurb "Add reusable task instructions." :page "skills" :title "Skills"}
    {:blurb "Send follow-ups, cancel a task and exit."
     :page "queue-and-cancel"
     :title "Controlling a session"}
    {:blurb "Exchange project messages and explicit pings between active sessions."
     :page "council"
     :title "Council"}
    {:blurb "Save or share a session." :page "exporting-sessions" :title "Exporting sessions"}
    {:blurb "Work in an isolated copy and land it on a branch." :page "drafts" :title "Drafts"}
    {:blurb "Use Vis from your phone or another machine."
     :page "gateway"
     :title "Remote access and the Companion app"}
    {:blurb "Report a problem without exposing private data."
     :page "reporting-bugs"
     :title "Reporting a bug"}]
   :section "Guides"}
  {:pages
   [{:blurb "Write a Python extension: tools, commands, guards and state."
     :page "extending"
     :title "Extending Vis"}
    {:blurb "Design typed tools from one tested package."
     :page "extension-design"
     :title "Extension design"}
    {:blurb "Install, publish and bundle skills with extensions."
     :page "extension-packages"
     :title "Extension packages"}
    {:blurb "Declarations, tool contracts and host operations."
     :page "extension-api"
     :title "Extension API"}
    {:blurb "Diagnose imports, registration and reload failures."
     :page "extension-troubleshooting"
     :title "Extension troubleshooting"}
    {:blurb "Show a typed form and read the answer." :page "human-input" :title "Asking the human"}
    {:blurb "Show progress a person can watch and stop." :page "live-views" :title "Live views"}
    {:blurb "Register an LLM provider from an extension."
     :page "provider-extensions"
     :title "Provider extensions"}]
   :section "Extensions"}
  {:pages [{:blurb "Why the agent writes programs and folds history."
            :page "token-optimization"
            :title "How Vis manages context"}
           {:blurb "Python execution, packages and permissions."
            :page "python-sandbox"
            :title "Python sandbox"}]
   :section "Concepts"}
  {:pages
   [{:blurb "Filesystem, environment and network rules for child processes."
     :page "jail"
     :title "Process jail and network policy"}
    {:blurb "Message and streaming formats." :page "content-blocks" :title "Content-block protocol"}
    {:blurb "Choose an installation method and update the runtime."
     :page "distributions"
     :title "Runtime distributions"}
    {:blurb "GraalVM build, metadata and TLS proxies."
     :page "jvm-native-image"
     :title "Building the native binary"}]
   :section "Reference"}]
 :site {:extension-center {:section "Community" :title "Extension Center"}
        :repo "https://github.com/Blockether/vis"
        :tagline "A coding agent for your projects."
        :title "Vis"}}
