{:nav
 [{:pages
   [{:blurb
     "Install Vis, choose a model and start a coding session that can edit your files and run tests."
     :page "index"
     :title "Getting started"}]
   :section nil}
  {:pages
   [{:blurb
     "Choose providers and models, and save settings for yourself, a project or every gateway client."
     :page "configuration"
     :title "Configuration"}
    {:blurb
     "Tell Vis how your project works, write reusable prompts and set a goal for a longer task."
     :page "context-and-prompts"
     :title "Project instructions"}
    {:blurb
     "Give Vis reusable instructions for reviews, releases and other tasks, with scripts and templates."
     :page "skills"
     :title "Skills"}
    {:blurb
     "Send follow-up messages while Vis works, edit queued requests, cancel a task or quit the session."
     :page "queue-and-cancel"
     :title "Controlling a session"}
    {:blurb
     "Ask another session for help, get a second review or divide a task between agents without repeating research."
     :page "council"
     :title "Council"}
    {:blurb
     "Save a conversation as Markdown or a self-contained HTML page, and check it before sharing."
     :page "exporting-sessions"
     :title "Exporting sessions"}
    {:blurb
     "Try a change in an isolated working copy, review the diff and decide whether to approve it."
     :page "drafts"
     :title "Drafts"}
    {:blurb
     "Connect your phone or another computer to your Vis gateway and continue working in the same sessions."
     :page "gateway"
     :title "Remote access and the Companion app"}
    {:blurb "Report a problem with useful evidence, without sharing credentials or private code."
     :page "reporting-bugs"
     :title "Reporting a bug"}]
   :section "Guides"}
  {:pages
   [{:blurb
     "Write a Python tool, register it with Vis and try it in a session, starting with one file."
     :page "extending"
     :title "Extending Vis"}
    {:blurb
     "Choose useful operations, explain their inputs and results, and test both Python code and Vis integration."
     :page "extension-design"
     :title "Extension design"}
    {:blurb
     "Install an extension for one project or all your projects, reload edits or package it to share."
     :page "extension-packages"
     :title "Installing and sharing extensions"}
    {:blurb
     "Connect an existing uv project to Vis while keeping its implementation editable in your checkout."
     :page "extension-development"
     :title "Using an existing Python project"}
    {:blurb
     "Look up Python declarations, callbacks, tool contracts and host operations for Vis extensions."
     :page "extension-api"
     :title "Extension API"}
    {:blurb
     "Find out why your extension will not load, a tool is missing or old code still runs after an edit."
     :page "extension-troubleshooting"
     :title "Extension troubleshooting"}
    {:blurb
     "Build a form that asks for a choice, credential or confirmation, and handle the answer."
     :page "human-input"
     :title "Forms and user input"}
    {:blurb "Show progress and controls while an extension watches a build or other ongoing work."
     :page "live-views"
     :title "Live views"}
    {:blurb
     "Connect an LLM service to Vis with custom sign-in, model discovery and credential management."
     :page "provider-extensions"
     :title "Provider extensions"}]
   :section "Extensions"}
  {:pages
   [{:blurb
     "Read the motivation for domain-specific tools, visible work and one session across devices."
     :page "motivation"
     :title "Why I built Vis"}
    {:blurb
     "See how Vis filters tool output, reuses findings and summarizes long conversations to manage context."
     :page "token-optimization"
     :title "How Vis manages context"}
    {:blurb "Understand where the agent runs Python, which packages it uses and what it can access."
     :page "python-sandbox"
     :title "Python sandbox"}]
   :section "Concepts"}
  {:pages
   [{:blurb
     "Limit file access, environment variables and network connections for commands Vis runs."
     :page "jail"
     :title "Process jail and network policy"}
    {:blurb
     "Install or update Vis, choose a release track and compare native releases with JVM source builds."
     :page "distributions"
     :title "Runtime distributions"}
    {:blurb
     "Build your own native Vis binary with GraalVM, check its metadata and configure a TLS proxy."
     :page "jvm-native-image"
     :title "Building the native binary"}]
   :section "Reference"}]
 :site {:extension-center {:section "Community" :title "Extension Center"}
        :repo "https://github.com/Blockether/vis"
        :tagline "Vis is a coding agent that combines tools into Python programs."
        :title "Vis"}}
