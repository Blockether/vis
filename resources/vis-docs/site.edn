{:nav
 [{:pages
   [{:blurb
     "Install Vis, choose a model provider and start your first coding session with an agent that combines tools into Python programs."
     :page "index"
     :title "Getting started"}]
   :section nil}
  {:pages
   [{:blurb
     "Configure Vis providers, models and project settings, and understand how configuration files combine across environments."
     :page "configuration"
     :title "Configuration"}
    {:blurb
     "Give Vis project instructions that explain your codebase, define task boundaries and keep the agent aligned with repository rules."
     :page "context-and-prompts"
     :title "Project instructions"}
    {:blurb
     "Add reusable task instructions to Vis with skills, and make established workflows available to the agent when they are needed."
     :page "skills"
     :title "Skills"}
    {:blurb
     "Control a running Vis session: send follow-up instructions, queue work, cancel an active task and exit when you are finished."
     :page "queue-and-cancel"
     :title "Controlling a session"}
    {:blurb
     "Coordinate sessions through asynchronous messages, delegate authorized work, review results and record improvement opportunities."
     :page "council"
     :title "Council"}
    {:blurb
     "Save and share Vis sessions with export options that preserve the conversation for review, collaboration and later reference."
     :page "exporting-sessions"
     :title "Exporting sessions"}
    {:blurb
     "Use Vis drafts to work in an isolated checkout, review the changes and land the completed work on the intended Git branch."
     :page "drafts"
     :title "Drafts"}
    {:blurb
     "Connect to Vis from your phone or another computer using the gateway and Companion app, with authenticated access to your sessions."
     :page "gateway"
     :title "Remote access and the Companion app"}
    {:blurb
     "Report a Vis problem with useful diagnostic details while keeping credentials, private project data and personal information out."
     :page "reporting-bugs"
     :title "Reporting a bug"}]
   :section "Guides"}
  {:pages
   [{:blurb
     "Build your first Vis extension in Python, choose the capability it provides and register tools that the agent can discover and call."
     :page "extending"
     :title "Extending Vis"}
    {:blurb
     "Design useful Python tools for Vis with clear declarations, readable activity labels, documented contracts and focused tests."
     :page "extension-design"
     :title "Extension design"}
    {:blurb
     "Install, reload and share Vis extensions from local projects or reviewed GitHub source, including projects in a monorepo."
     :page "extension-packages"
     :title "Installing and sharing extensions"}
    {:blurb
     "Connect an existing Python project to Vis with uv, editable dependencies and a development workflow for testing extension changes."
     :page "extension-development"
     :title "Using an existing Python project"}
    {:blurb
     "Reference the Vis extension API for declarations, tool contracts, host operations and the Python interfaces used to register capabilities."
     :page "extension-api"
     :title "Extension API"}
    {:blurb
     "Troubleshoot Vis extensions by diagnosing Python imports, dependency setup, tool registration and changes that fail to reload."
     :page "extension-troubleshooting"
     :title "Extension troubleshooting"}
    {:blurb
     "Ask the human for structured input from a Vis extension, present a typed form and read the answer before continuing the task."
     :page "human-input"
     :title "Asking the human"}
    {:blurb
     "Create live views in Vis extensions to show ongoing progress, present updates and let the human watch or stop an operation."
     :page "live-views"
     :title "Live views"}
    {:blurb
     "Register an LLM provider from a Vis extension and connect its models to the agent through the supported provider interfaces."
     :page "provider-extensions"
     :title "Provider extensions"}]
   :section "Extensions"}
  {:pages
   [{:blurb
     "Understand how Vis manages model context by combining tool calls into Python programs and folding completed work from conversation history."
     :page "token-optimization"
     :title "How Vis manages context"}
    {:blurb
     "Understand the Vis Python sandbox, including execution, available packages, project environments and permissions for host operations."
     :page "python-sandbox"
     :title "Python sandbox"}]
   :section "Concepts"}
  {:pages
   [{:blurb
     "Reference the Vis process jail and its filesystem, environment and network rules for commands and child processes started by the agent."
     :page "jail"
     :title "Process jail and network policy"}
    {:blurb
     "Reference the Vis content-block protocol for structured messages, streamed updates and the formats shared between the engine and clients."
     :page "content-blocks"
     :title "Content-block protocol"}
    {:blurb
     "Choose a Vis runtime distribution, install the required components and understand how to update the engine for your environment."
     :page "distributions"
     :title "Runtime distributions"}
    {:blurb
     "Build the Vis native binary with GraalVM, configure reachability metadata and handle TLS proxies in the native-image build workflow."
     :page "jvm-native-image"
     :title "Building the native binary"}]
   :section "Reference"}]
 :site {:extension-center {:section "Community" :title "Extension Center"}
        :repo "https://github.com/Blockether/vis"
        :tagline "Vis is a coding agent that combines tools into Python programs."
        :title "Vis"}}
