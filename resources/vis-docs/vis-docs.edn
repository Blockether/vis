{:site {:title "Vis"
        :tagline "A coding agent that treats context as an environment, not a transcript."
        :repo "https://github.com/Blockether/vis"
        :headline "Context is an environment, not a transcript."
        :sub "Other agents shovel every message into a growing window and panic-compact when it overflows. Vis keeps state in a real runtime the model talks to through code — vars, a database, query results — so the window holds only what the model needs right now. No emergency compaction. No sliding windows. Works with any text model."
        :cta {:label "Get started" :slug "token-optimization"}}
 :pages [{:file "index.md"             :title "Introduction"        :section nil          :order 0}
         {:file "token-optimization.md" :title "Token optimization" :section "How it works" :order 10}
         {:file "graalpython.md"        :title "GraalPython sandbox" :section "How it works" :order 20}
         {:file "jvm-native-image.md"   :title "JVM & native-image"  :section "Runtime"      :order 30}
         {:file "distributions.md"      :title "Custom distributions" :section "Runtime"     :order 40}
         {:file "configuration.md"      :title "Configuration"        :section "Using Vis"   :order 50}
         {:file "context-and-prompts.md" :title "Context files & prompts" :section "Using Vis" :order 55}
         {:file "exporting-sessions.md" :title "Exporting sessions" :section "Using Vis" :order 58}
         {:file "skills.md"             :title "Skills"                :section "Using Vis"   :order 60}
         {:file "gateway.md"            :title "Gateway & pairing"    :section "Using Vis"   :order 65}
         {:file "python-extensions.md"  :title "Python extensions"    :section "Extensions"  :order 70}
         {:file "extending.md"          :title "Clojure extensions"   :section "Extensions"  :order 80}]}
