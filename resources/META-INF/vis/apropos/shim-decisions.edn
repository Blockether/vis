;; GENERATED from sandbox Python docstrings; regenerate with apropos-resource-test.
[
 {:name "vis_decisions", :kind "module", :text "Read decision models through Vis' authenticated gateway without installing third-party packages. Use the full vis-agent SDK for training, upload, aliases and remote client connections. Downloads always require an explicit CLI command.\n\nClasses:\n  DecisionGatewayError(status, code) — A decision request failed; inspect status and code without logging the body.\n\nFunctions:\n  infer(*, model, state, questions, timeout=None) — Answer choice, score and noul questions with a named installed model.\n  model(model_ref, *, timeout=None) — Read the status of one immutable decision version or named baseline.\n  models(*, timeout=None) — List installed decision versions and their residency without downloading.\n\nRead one member: doc(\"vis_decisions.DecisionGatewayError\")"}
 {:name "vis_decisions.DecisionGatewayError", :kind "class", :text "A decision request failed; inspect status and code without logging the body.", :call "DecisionGatewayError(status, code)"}
 {:name "vis_decisions.infer", :kind "function", :text "Answer choice, score and noul questions with a named installed model.", :call "infer(*, model, state, questions, timeout=None)"}
 {:name "vis_decisions.model", :kind "function", :text "Read the status of one immutable decision version or named baseline.", :call "model(model_ref, *, timeout=None)"}
 {:name "vis_decisions.models", :kind "function", :text "List installed decision versions and their residency without downloading.", :call "models(*, timeout=None)"}]
