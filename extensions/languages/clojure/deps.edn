{:paths ["src" "resources"]
 :deps {com.blockether/vis {:local/root "../../.."}
        ;; rewrite-clj — structured Clojure source editing. The whole
        ;; reason this extension exists: AST-aware patches that survive
        ;; whitespace/comment/formatting drift.
        rewrite-clj/rewrite-clj {:mvn/version "1.2.50"}}}
