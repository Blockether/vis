total = first_value + second_value * third_value - fourth_value / fifth_value + sixth_value
condition = alpha_condition and beta_condition or gamma_condition and not delta_condition_value
comparison = lower_bound <= value < upper_bound and value not in excluded and value is not None
power = x**2 + y ** -1 + z**f(a) + 5 ** 5.0 + (-a)**b + x**y.z
sliced = items[1 : 2], items[lower+offset : upper+offset], items[::2], items[fn(x) : fn(y)]
unary = - x, + y, ~ z, not w
conditional = value_if_true_long_name if some_condition_that_is_long else value_if_false_long_name
if (n := len(items)) > 10: pass
subscript = mapping["key"]["nested"][0]
attribute = obj . attr . method( )
starred = [*first, *second]
merged = {**defaults, **overrides}
generator = sum(x*x for x in range(10))
negated = not(a)
parenthesized = (((value)))
assert isinstance(value, int), f"expected int but got {type(value).__name__} for {name}"
very_long_variable_name = some_object.some_attribute.some_method(argument).another_attribute[index]
