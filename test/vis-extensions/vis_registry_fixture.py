"""A registry fixture, not a surface Vis ships.

`extensions_test` registers this file to prove the engine materializes exactly
what a language pack registered. Vis itself ships no extension source: the
language packs do.
"""

MARKER = "registry fixture"
